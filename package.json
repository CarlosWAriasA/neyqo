{
  "name": "neyqo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:preview": "npm --prefix frontend run preview",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "backend:start": "npm --prefix backend run start",
    "worker:dev": "npm --prefix neyqo-worker run dev",
    "worker:build": "npm --prefix neyqo-worker run build",
    "worker:start": "npm --prefix neyqo-worker run start",
    "worker:test": "npm --prefix neyqo-worker run test",
    "android:build": "gradlew.bat :app:assembleDebug",
    "build": "npm run frontend:build && npm run backend:build && npm run worker:build",
    "lint": "npm run frontend:lint"
  }
}
